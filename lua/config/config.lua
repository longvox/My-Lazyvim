-- ~/.config/nvim/lua/config.lua

return {
  terminal = {
    direction = "horizontal",  -- or "vertical" or "float"
    size = 20,  -- Set the size of the terminal window
    position = "bottom",  -- Position the terminal at the bottom
  },
}

